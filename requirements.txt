fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
aiosqlite>=0.20
alembic>=1.14
pydantic>=2.10
pydantic-settings>=2.6
python-jose[cryptography]>=3.3
bcrypt>=4.0
email-validator>=2.2
python-multipart>=0.0.17
llama-index>=0.10.0
PyMuPDF>=1.23.0
# Pengekstrak markdown resmi PyMuPDF: menyambung paragraf, mengenali tingkat
# heading dan tabel, serta menaruh gambar pada posisinya saat impor PDF.
pdf2docx>=0.5.8,<0.6
markitdown[pdf,docx]>=0.1.7
pymupdf4llm>=1.28.0
faiss-cpu>=1.8.0
openai>=1.10.0
chromadb>=0.4.0
# Penerus `duckduckgo-search`; paket lama tetap ada sebagai cadangan impor.
ddgs>=9.0
duckduckgo-search>=5.0
llama-index-vector-stores-chroma>=0.1.0
python-docx>=1.1.0
# Mesin cetak PDF laporan (Chromium headless). Setelah dipasang, browsernya
# perlu diunduh sekali: `python -m playwright install chromium`.
playwright>=1.61.0
# Dipakai LlamaIndex `SimpleDirectoryReader` untuk mengekstrak teks dari .docx.
docx2txt>=0.8
# Uji: berkas di tests/ memakai pytest + penanda @pytest.mark.anyio untuk test async.
pytest>=8.0
anyio>=4.0
